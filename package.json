{
  "name": "ng-landing",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "npm-run-all --parallel css:watch eleventy:serve",
    "eleventy:serve": "npx eleventy --serve",
    "build": "npx eleventy",
    "test": "echo \"Error: no test specified\" && exit 1",
    "css:build": "sass src/css/styles.scss:src/css/styles.css --no-source-map --style=compressed",
    "css:watch": "sass --watch src/css/styles.scss:src/css/styles.css --no-source-map"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "bootstrap": "^5.3.6",
    "npm-run-all": "^4.1.5",
    "sass": "^1.87.0"
  },
  "dependencies": {
    "p5": "^2.0.2"
  }
}
