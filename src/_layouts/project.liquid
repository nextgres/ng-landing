---
layout: default
body_classes: page-projects
---

{% if hero.enabled %}
  {% include "framework/hero", 
    heading:hero.heading 
    sub_heading:hero.sub_heading 
    text_color:hero.text_color 
    background_image:hero.background_image 
    background_image_blend_mode:hero.background_image_blend_mode 
    background_color:hero.background_color 
    background_gradient:hero.background_gradient 
    fullscreen_mobile:hero.fullscreen_mobile 
    fullscreen_desktop:hero.fullscreen_desktop 
    height:hero.height 
    buttons:hero.buttons 
    image:hero.image
  %}
{% else %}
  <div class="section">
    <div class="container">
    {% include "framework/title", 
      fa_icon:fa_icon 
      icon:icon 
      title:title 
      description:description 
      image:image 
    %}
    </div>
  </div>
{% endif %}

<div class="section strip-border-top">
  <div class="container">
    <div class="row justify-content-center">

      <div class="col-12 col-lg-8">
        {% if site.enable_breadcrumbs_projects %}
          {% include "framework/breadcrumbs" %}
        {% endif %}
        <div class="content">{{ content }}</div>
      </div>

      {% if site.enable_sidebar_projects %}
        <div class="col-12 col-lg-3 offset-lg-1">
          {% include "framework/sidebar", collection:"projects" %}
        </div>
      {% endif %}

    </div>
  </div>
</div>