<div class="menu-bottom">
  <ul>
    {% for item in menu.bottom %}
      <li class="menu-item-{{ item.name }}">
        <a href="{{ item.url }}">{{ item.name }}</a>
      </li>
    {% endfor %}
  </ul>
</div>