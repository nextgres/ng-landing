{% assign limit = page.limit | default:10 %}

{% if social %}
<div class="social" id="social">
  {% for social in social limit:limit %}
  <a href="{{ social.url }}" target="blank" title="{{ social.title }}">
    <i class="{{ social.fa_icon }}"></i>
  </a>
  {% endfor %}
</div>
{% endif %}