{% assign cards = site[collection] %}
{% assign limit = limit | default: nil %}
{% assign sort_by = sort | default: 'date' %}
{% assign view_more_button_text = view_more_button_text | default: "View More" %}
{% assign view_more_button_align = view_more_button_align | default: "right" %}
{% assign columns = columns | default: 3 %}

{% capture columns %}col-lg-{{ 12 | divided_by:columns }}{% endcapture %}
{% assign cards = cards | sort: 'date' | 'reverse' %}
{% if sort_by == 'weight' %}
  {% assign cards = cards | sort: 'weight' %}
{% endif %}

{% if cards.size > 0 %}
  <div class="row grid">

    {% if heading or sub_heading %}
      <div class="col-12">
        <div class="grid-heading">
          {% if heading != "" %}<h2>{{ heading }}</h2>{% endif %}
          {% if sub_heading and sub_heading != "" %}<p class="large">{{ sub_heading }}</p>{% endif %}
        </div>
      </div>
    {% endif %}

    {% for card in cards limit: limit %}
      {% case collection %}
        {% when 'posts' %}
          <div class="col-12 col-md-6 {{ columns }} mb-gutter mt-gutter {% if forloop.first %}mt-0{% endif %} {% if forloop.last %}mb-0{% endif %} mt-md-0">
            {% include 'theme/cards/card-post',
              title:card.data.title
              thumbnail:card.data.thumbnail
              url:card.data.url
            %}
          </div>
        {% when 'projects' %}
          <div class="col-12 col-md-6 {{ columns }} mb-gutter mt-gutter {% if forloop.first %}mt-0{% endif %} {% if forloop.last %}mb-0{% endif %} mt-md-0">
            {% include 'theme/cards/card-project',
              title:card.data.title
              description:card.data.description
              thumbnail:card.data.thumbnail
              authors:card.data.authors
              categories:card.data.categories
              date:card.date
              url:card.url
              weight:card.data.weight
              client:card.data.client
              icon:card.data.icon
              fa_icon:card.data.fa_icon
            %}
          </div>
        {% else %}
        <div class="col-12 col-md-6 {{ columns }} mb-gutter mt-gutter {% if forloop.first %}mt-0{% endif %} {% if forloop.last %}mb-0{% endif %} mt-md-0">
            {% include 'theme/cards/card-post',
              title:card.data.title
              description:card.data.description
              thumbnail:card.data.thumbnail
              url:card.data.url
            %}
          </div>
      {% endcase %}
    {% endfor %}

    <div class="col-12">
      {% if view_more_button_link and view_more_button_link != '' %}
        <div class="grid-footer grid-footer-align-{{ view_more_button_align }}">
          <a href="{{view_more_button_link }}" class="grid-view-more-button">
            {{ view_more_button_text }} <i class="fa-solid fa-arrow-right"></i>
          </a>
        </div>
      {% endif %}
    </div>

  </div>
{% endif %} 