<section class="section-solution">
  <div class="container">
    <div class="section-solution-grid">

      <!-- LEFT COLUMN — Copy -->
      <div class="section-solution-copy">
        <h2 class="section-solution-headline">Your data is already enough.</h2>

        <p class="section-solution-subhead"><strong>NEXTGRES doesn't replace your infrastructure. It sits on top of it.</strong></p>

        <p class="section-solution-body">One virtualization layer connects read-only to your SQL databases, NoSQL stores, and event streams. It determines which behavioral signals matter and ingests only what's needed — no pipelines to build, no data warehouse required.</p>

        <p class="section-solution-body section-solution-body--tight">When a user's behavior changes mid-session — a plan upgrade, a skipped episode, an abandoned cart — NEXTGRES sees the signal and acts on it instantly, across every touchpoint.</p>

        <ul class="section-solution-proofs">
          <li>Read-only connections — your source systems are never modified</li>
          <li>Schema-agnostic — works regardless of how your data is structured</li>
          <li>Sub-second inference — acts on behavioral signals as they happen</li>
        </ul>
      </div>

      <!-- RIGHT COLUMN — Architecture Diagram -->
      <div class="section-solution-diagram">
        <svg viewBox="0 0 620 310" xmlns="http://www.w3.org/2000/svg" class="section-solution-svg">

          <!-- Column labels -->
          <text x="65" y="20" text-anchor="middle" fill="#ffffff" font-size="11" letter-spacing="0.5" font-weight="600">YOUR DATA</text>
          <text x="310" y="20" text-anchor="middle" fill="#ffffff" font-size="11" letter-spacing="0.5" font-weight="600">NEXTGRES</text>
          <text x="545" y="20" text-anchor="middle" fill="#ffffff" font-size="11" letter-spacing="0.5" font-weight="600">WHAT USERS SEE</text>

          <!-- ====== LEFT COLUMN: Data source cards ====== -->

          <!-- SQL databases -->
          <rect x="0" y="40" width="130" height="60" rx="8" fill="#0D2147" stroke="#2A4A80" stroke-width="1"/>
          <text x="14" y="64" fill="#fff" font-size="14" font-weight="500">SQL databases</text>
          <text x="14" y="82" fill="#C0D0E8" font-size="12">Postgres, MySQL</text>

          <!-- NoSQL stores -->
          <rect x="0" y="118" width="130" height="60" rx="8" fill="#0D2147" stroke="#2A4A80" stroke-width="1"/>
          <text x="14" y="142" fill="#fff" font-size="14" font-weight="500">NoSQL stores</text>
          <text x="14" y="160" fill="#C0D0E8" font-size="12">Mongo, Dynamo</text>

          <!-- Event streams -->
          <rect x="0" y="196" width="130" height="60" rx="8" fill="#0D2147" stroke="#2A4A80" stroke-width="1"/>
          <text x="14" y="220" fill="#fff" font-size="14" font-weight="500">Event streams</text>
          <text x="14" y="238" fill="#C0D0E8" font-size="12">Kafka, Kinesis</text>

          <!-- ====== Arrows: Left → Center ====== -->
          <defs>
            <marker id="arrow" viewBox="0 0 8 8" refX="7" refY="4" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
              <path d="M0,0 L8,4 L0,8 Z" fill="#4D9FFF"/>
            </marker>
          </defs>

          <!-- Top source → center -->
          <line x1="130" y1="70" x2="218" y2="118" stroke="rgba(77,159,255,0.5)" stroke-width="1.5" marker-end="url(#arrow)"/>
          <!-- Middle source → center -->
          <line x1="130" y1="148" x2="218" y2="148" stroke="rgba(77,159,255,0.5)" stroke-width="1.5" marker-end="url(#arrow)"/>
          <!-- Bottom source → center -->
          <line x1="130" y1="226" x2="218" y2="178" stroke="rgba(77,159,255,0.5)" stroke-width="1.5" marker-end="url(#arrow)"/>

          <!-- ====== CENTER: NEXTGRES layer ====== -->
          <rect x="224" y="78" width="172" height="140" rx="10" fill="#0D2147" stroke="#2260D9" stroke-width="1.5"/>

          <text x="310" y="138" text-anchor="middle" fill="#fff" font-size="18" font-weight="600">Virtualization</text>
          <line x1="252" y1="148" x2="368" y2="148" stroke="rgba(255,255,255,0.2)" stroke-width="1"/>
          <text x="310" y="165" text-anchor="middle" fill="#C0D0E8" font-size="13">Unified Data Engine</text>
          <text x="310" y="183" text-anchor="middle" fill="#C0D0E8" font-size="13">Personalization Platform</text>

          <!-- Pulsing dot -->
          <circle cx="242" cy="200" r="3.5" fill="#5DCAA5" class="section-solution-pulse"/>

          <!-- ====== Arrows: Center → Right ====== -->

          <!-- Center → top output -->
          <line x1="396" y1="118" x2="462" y2="70" stroke="rgba(77,159,255,0.5)" stroke-width="1.5" marker-end="url(#arrow)"/>
          <!-- Center → middle output -->
          <line x1="396" y1="148" x2="462" y2="148" stroke="rgba(77,159,255,0.5)" stroke-width="1.5" marker-end="url(#arrow)"/>
          <!-- Center → bottom output -->
          <line x1="396" y1="178" x2="462" y2="226" stroke="rgba(77,159,255,0.5)" stroke-width="1.5" marker-end="url(#arrow)"/>

          <!-- ====== RIGHT COLUMN: Output cards ====== -->

          <!-- Recommendations -->
          <rect x="468" y="40" width="150" height="60" rx="8" fill="#E6F1FB" stroke="#85B7EB" stroke-width="1"/>
          <text x="543" y="64" text-anchor="middle" fill="#0C447C" font-size="14" font-weight="500">Recommendations</text>
          <text x="543" y="82" text-anchor="middle" fill="#0C447C" font-size="12" opacity="0.8">Personalized per user</text>

          <!-- Content -->
          <rect x="468" y="118" width="150" height="60" rx="8" fill="#E1F5EE" stroke="#5DCAA5" stroke-width="1"/>
          <text x="543" y="142" text-anchor="middle" fill="#085041" font-size="14" font-weight="500">Content</text>
          <text x="543" y="160" text-anchor="middle" fill="#085041" font-size="12" opacity="0.8">Tailored in real time</text>

          <!-- Search -->
          <rect x="468" y="196" width="150" height="60" rx="8" fill="#FEF3E2" stroke="#F0B95A" stroke-width="1"/>
          <text x="543" y="220" text-anchor="middle" fill="#7A5A1E" font-size="14" font-weight="500">Search</text>
          <text x="543" y="238" text-anchor="middle" fill="#7A5A1E" font-size="12" opacity="0.8">Ranked individually</text>

          <!-- Bottom annotation -->
          <text x="310" y="295" text-anchor="middle" fill="#A8BCD8" font-size="13">No pipelines. No warehouse. Just the signals that matter.</text>

        </svg>
      </div>

    </div>
  </div>
</section>
